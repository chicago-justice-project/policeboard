# buildspec.yml
version: 0.2

phases:
  build:
    commands:
      - echo Build started on `date`
      - echo Building the zip file...
      - ./build.sh
      - ls -ld policeboard

artifacts:
  files: policeboard