# Policeboard nginx tweaks. Included inside the default server{} block via
# .platform/nginx/conf.d/elasticbeanstalk/. Platform's webapp.conf already
# serves /assets, so we don't redefine it here.
client_max_body_size 20M;
