<%#
# Form Partial

This partial is rendered on a resource's `new` and `edit` pages,
and renders all form fields for a resource's editable attributes.

## Local variables:

- `page`:
  An instance of [Administrate::Page::Form][1].
  Contains helper methods to display a form,
  and knows which attributes should be displayed in the resource's form.

[1]: http://www.rubydoc.info/gems/administrate/Administrate/Page/Form
%>

<%= form_for([namespace, page.resource], html: { class: "form" }) do |f| %>
  <% if page.resource.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(page.resource.errors.count, "error") %>
        prohibited this <%= page.resource_name %> from being saved:
      </h2>

      <ul>
        <% page.resource.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
  <% page.attributes.each do |attribute| -%>
    <div class="field-unit field-unit--<%= attribute.html_class %>">
      <%= render_field attribute, f: f %>
    </div>
  <% end -%>
  
  


  <% if false %>
  <% page.attributes.each do |attribute| -%>
    <div class="field-unit field-unit--<%= attribute.html_class %>">
      <%= attribute %>
      <% if attribute.attribute == :recommended_outcome %>
      	<%= render_field attribute, f: f %>
      <% end %>
      <!-- <%= render_field attribute, f: f %> -->
    </div>
  <% end -%>
  <% end %>

  
  <hr />
  <h2>Case summary </h2>
  
  <div class="field">
	  <div class="label">Time frame</div>
	  <div>
		  <%= render_field page.attributes.detect {|a| a.attribute == :date_initiated }, f: f  %>
	  </div>
   </div>
   
   <hr />
  
   <h2>Rule violations</h2>
   
  
  <p>Violated rules</p>
  <ul>
  <% page.resource.case_rules.each do |case_rule | -%>
  	 <li>
  	 	<strong><%= "Rule #{case_rule.rule.code} (#{case_rule.case_rule_counts.count} counts): " %></strong>
  	 	<%= "#{case_rule.rule.description}" %>
  	 	
  	 	<% case_rule.case_rule_counts.order(:count_order).each do | case_rule_count | -%>
  	 		<div class="count-rule">
	  	 		<span class="count-order"><%= "Count #{case_rule_count.count_order}:" %></span>
	  	 		<span class="is-guilty"><%= "#{case_rule_count.is_guilty}"%></span>
	  	 		<p><%= case_rule_count.content %></p>
  	 		</div>
  	 	<% end -%>
  	 </li>
  <% end -%>
  </ul>

  <div class="form-actions">
    <%= f.submit %>
  </div>
<% end %>
